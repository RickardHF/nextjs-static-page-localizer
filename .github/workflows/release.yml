name: Release Package

on:
    workflow_dispatch:

jobs:
    Publish-Release:
        runs-on: ubuntu-latest
        steps:
            - name: Download Artifact
              uses: actions/download-artifact@v3
              with:
                name: artifact
            
            - name: List Files
              run: ls -R